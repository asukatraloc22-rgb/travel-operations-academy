# shared/

Tout ce qui est réutilisé par plusieurs modules :
- composants UI génériques (bouton, carte, badge...)
- fonctions utilitaires
- hooks partagés

Si un composant est utilisé par un seul module, il reste dans ce module,
pas ici.
